#! joomla-joomla_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: joomla-joomla-data
    namespace: pythonproject
    labels:
        com.docker.compose.service: joomla
        com.docker.compose.volume: joomla_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
